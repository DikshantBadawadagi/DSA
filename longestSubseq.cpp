#include<bits/stdc++.h>
using namespace std;

int main() {
    vector<int> nums = {3,8,5,7,6};
    int n = nums.size();
    sort(nums.begin(),nums.end());
    vector<int> ans;
    int count=0;
    for(int i=0;i<n;i++) {
        if(nums[i+1] == nums[i] + 1){
            

        }
    }
}
